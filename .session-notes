# Session Notes — 2026-04-08 (Session 15)

### Read These Files First (in order)

1. `CLAUDE.md` — 10-module structure; M4/M9/M10 titles were corrected this session
2. `git log --oneline 13c321c..HEAD` — 9 commits (LFS, titles, M7/M8 re-strip, M9/M10 redesign, fixes, COC sync)
3. `workspaces/curriculum/m9-m10-redesign-plan.md` — redesign rationale and new exercise map
4. `tests/conftest.py` — SLOW_EXERCISES now 11 entries (5 M10 added this session)
5. `tests/test_datasets.py` — 38 EXPECTED_DATASETS (safety_prompts.parquet added)

## Accomplished

- **M9/M10 full exercise redesign** — 16 new solutions (8876 lines). M9 pivoted from LLMs/RAG (M5 duplicate) to fine-tuning internals (LoRA math, DPO derivation, GRPO, CAI, quantization, reward hacking, red-teaming). M10 pivoted from governance basics (M6 duplicate) to enterprise scale (multi-org, federated learning, privacy, adversarial robustness, compliance automation).
- **M7/M8 re-stripped to 20-25%** target (was 14-16% avg). All 16 exercises + 32 notebooks regenerated.
- **Git LFS activated** — 39 data files tracked, .gitattributes created. 2 new datasets (safety_prompts.parquet, sg_regulatory_orgs.yaml).
- **CLAUDE.md + 3 deck titles fixed** — M4→"Unsupervised ML, Anomaly Detection & NLP", M9→"Fine-Tuning, Alignment & RL", M10→"AI Governance, Safety & Enterprise".
- **12 SDK issues filed** (kailash-py #341-348, kailash-rs #240-243). All still OPEN (verified: `gh issue view` on each, zero comments).
- **Runtime errors fixed** — kaizen imports (`from kaizen.core import Signature` → `from kaizen import Signature`), DriftMonitor API (requires ConnectionManager + async set_reference), DataFlow paths (→ sqlite:///:memory:), sampling bug.
- **COC synced to 3.1.0** from template (was 3.0.0). 22 files updated.
- **Tests**: 1121/1121 static, 69/69 non-slow solutions passing.

## Outstanding

- [ ] **12 SDK issues still open** — kailash-py #341-348, kailash-rs #240-243. kailash_ml 0.7.0 has none of the requested features (verified: `from kailash_ml import metrics` fails; no cluster/reduction/text engines). 14 M3/M4 sklearn violations remain accepted until shipped.
- [ ] **6 M10 sklearn files** — `modules/ascent10/solutions/ex_2-5,7,8.py` import sklearn for from-scratch implementations (FedAvg, k-anonymity, FGSM, etc.). Pedagogical, not wrappable — these teach algorithm internals.
- [ ] **decks/ascent01 drift** — H2 still says "Data Pipelines & Visualisation Mastery with Python" vs CLAUDE.md "Foundations — Statistics, Probability & Data" (verified: `grep '<h2' decks/ascent01/deck.html`).
- [ ] **11 slow exercises** — 6 original + 5 new M10 (ex_2/4/6/7/8). Not bugs — heavy computation. Could optimize M10 by reducing iteration counts.
- [ ] **Notebook end-to-end validation** — 240 notebooks regenerated but never executed.
- [ ] **Hint coverage** — re-measure after all re-strips.
- [ ] **Git LFS history cleanup** — old data files still as raw blobs in history. Would require force-push.

## Oversight — Verify Before Starting

- [ ] `git log --oneline -1` should be `d2ee21b chore: sync COC artifacts to template v3.1.0`
- [ ] `uv run pytest tests/test_consistency.py tests/test_imports.py tests/test_datasets.py -q` → **1121 passed** (verified: 1121 passed in 5.59s)
- [ ] `cat .claude/VERSION | grep version | head -1` → `"version": "3.1.0"` (verified)

## Oversight — Verify After Completing

- [ ] `uv run pytest tests/test_solutions.py -m "not slow" -q` passes cleanly (69 tests, ~13 min)
- [ ] No new framework violations: `grep -rlE "^from sklearn|^import sklearn" modules/ascent09/solutions | wc -l` → 0 (verified: 0)
- [ ] M10 sklearn count stable: `grep -rlE "^from sklearn|^import sklearn" modules/ascent10/solutions | wc -l` → 6 (verified: 6, pedagogical)

## Blockers

- **SDK API gaps** — all 12 issues unfixed. M3/M4 exercises cannot drop sklearn until kailash-ml ships metrics (#341), clustering (#344), reduction (#345), text (#346). Ping the devs.
