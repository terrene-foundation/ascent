# Session Notes -- 2026-04-06 (Session 9)

### Read These Files First (in order)

1. `CLAUDE.md` -- 10-module ASCENT structure, absolute directives, updated course structure table
2. `README.md` -- Programme overview
3. `workspaces/curriculum/briefs/expanded-curriculum-v5.md` -- NEW: 10-module curriculum (replaces v4)
4. `workspaces/curriculum/02-plans/01-implementation-plan.md` -- Updated to v5, all phases tracked
5. `rules/exercise-standards.md` -- exercise format, progressive scaffolding targets
6. `scripts/py_to_notebook.py` -- .py→.ipynb converter

## Accomplished (Session 9)

- **Build fix**: `pyproject.toml` lacked `[tool.hatch.build.targets.wheel] packages = ["shared"]` — `uv sync` now works
- **10 deck PDFs**: All generated via decktape (M1-M8, M10) + Playwright fallback (M9). Sizes 2-10MB
- **Curriculum v5**: `expanded-curriculum-v5.md` — 10 modules × 8 lessons = 80, Foundation M1-M6 + Advanced M7-M10
- **CLAUDE.md updated**: Course structure table now matches actual module titles and frameworks; scaffolding table extended to M10
- **Implementation plan v5**: `01-implementation-plan.md` updated from v4 (48 exercises) to v5 (80 exercises)
- **Textbook complete**: 83 Python → 83 Markdown → 83 HTML (was 83/33/26). All 9 sections at full parity
- **Red team round 2**: 3 exercise agents + quiz reviewer + curriculum reviewer ran in parallel. Fixed:
  - M7 ex_3/ex_4: Training loops had no backpropagation (flat loss curves). Added full backward pass
  - M9 ex_7: Wrong import `from kailash_mcp` → `from kailash.mcp_server`
  - All modules: Standardized `kailash_pact` → `pact`, `kailash_nexus` → `nexus` (118 files touched)
  - Quiz 10.A.3: Optimizer state math (16.8 MB → 33.6 MB, ratio 1667× → 833×)
  - Quiz 10.D.3: Quantization explanation (metadata can't halve size — now cites GPTQ group quantization)
- **NOT yet committed**: 118 modified files in working tree

## Outstanding — Next Session Work Queue

### P1: Commit and Push
- 118 modified files need staging and committing (verified: `git status --short | wc -l` = 118)
- Logical commit split: (1) build fix, (2) curriculum v5 + plan, (3) red team fixes, (4) textbook completion

### P2: Speaker Note Time Calibration
- ALL modules overrun 180min target: M1=882min, M7=497min, M8=473min, M9=494min, M10=542min
- Systemic issue from session 8 — not addressed this session
- Options: add skip markers, reduce per-slide times, or accept longer delivery

### P2: Deck Audit Script Recalibration
- `scripts/deck-audit.mjs` reports 84-98% overflow on ALL decks (M1 included) — systemic false positive
- Screenshots show slides render correctly; the script measures Reveal.js transformed elements incorrectly

### P2: M1-M6 Module READMEs Say Wrong Exercise Counts
- READMEs say "5 Exercises" or "6 Exercises" but each module now has 8 exercise files
- (verified: `head -10 modules/ascent01/README.md` says "Lab (3h) — 5 Exercises")

### P3: Quiz Parity M1-M6 vs M7-M10
- M1-M6: 24-25 questions each; M7-M10: 16 questions each
- Consider adding 8-9 more questions per M7-M10 quiz for consistency

## Oversight — Verify Before Starting

- [ ] 80 exercises: `find modules/ascent*/solutions -name "ex_*.py" | wc -l` = 80 (verified: 80)
- [ ] 10 PDFs: `ls decks/ascent*/deck.pdf | wc -l` = 10 (verified: 10)
- [ ] 83 textbook: `find textbook/markdown -name "*.md" | wc -l` = 83 (verified: 83)
- [ ] No wrong imports: `grep -rn "kailash_pact\|kailash_nexus\|kailash_mcp" --include="*.py" modules/ | wc -l` = 0 (verified: 0)
- [ ] Curriculum v5: `head -1 workspaces/curriculum/briefs/expanded-curriculum-v5.md` exists (verified)

## Oversight — Verify After Completing

- [ ] Commit: `git log --oneline -3` shows session 9 commits
- [ ] Push: `git push origin main` successful
- [ ] Clean: `git status --short | wc -l` = 0

## Blockers

- M9 deck PDF required Playwright fallback (decktape fails on M9 — Reveal.js version detection bug)
- decktape version: npx decktape (global install), Playwright 1.59.1 in node_modules
